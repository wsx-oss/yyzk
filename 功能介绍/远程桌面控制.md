# 远程桌面控制

## 1. 功能概述

远程桌面控制模块是"CloudControl"的核心功能之一，用于管理和操控远程设备的桌面连接。该模块支持远程连接的新增、搜索、刷新、连接、断开、删除等操作，并通过统计图表直观展示连接状态分布和连接类型使用情况，帮助管理人员高效管理远程设备。

> **架构变更**：在当前版本中，远程设备统一由 **无人机管理模块** 创建和管理——注册无人机时系统会自动在 `devices` 表中生成对应的远程设备记录（包含 IP、端口、协议等信息）。远程桌面控制页面的设备列表**仅展示与无人机关联的设备**（`drone_id > 0`），不再显示独立手动添加的设备。建议通过无人机管理模块统一注册设备。

## 2. 功能详细说明

### 2.1 连接管理功能

#### 新增连接

用户可点击页面右上角的 **"新增连接"** 按钮，打开新增远程连接的弹窗。在该窗口中，用户需填写以下信息：

| 字段     | 是否必填 | 属于谁     | 说明                                                                            |
| -------- | -------- | ---------- | ------------------------------------------------------------------------------- |
| 设备名称 | 是       | 目标设备   | 远程设备的标识名称，如"Server01"、"测试机A"                                     |
| IP地址   | 是       | 目标设备   | 目标设备的局域网或公网 IP 地址，如 `192.168.1.100`                            |
| 连接类型 | 是       | 连接协议   | 连接目标设备所用的协议：RDP（Windows远程桌面）、VNC（跨平台）、SSH（Linux终端） |
| 端口号   | 是       | 目标设备   | 目标设备上远程服务监听的端口号。VNC 默认 5900，RDP 默认 3389，SSH 默认 22       |
| 用户名   | 是       | 目标设备   | 登录目标设备的系统账户用户名（即目标电脑上的操作系统用户名）                    |
| 密码     | 否       | 目标设备   | 登录目标设备的系统账户密码（即目标电脑上的操作系统密码）                        |
| 自动连接 | 否       | 本系统设置 | 启用后，系统启动时会自动尝试连接该设备                                          |
| 记录日志 | 否       | 本系统设置 | 启用后，系统会记录该设备的连接/断开等操作日志                                   |
| 描述信息 | 否       | 备注       | 对该设备的补充说明，如"二楼机房主服务器"、"无人机控制终端"                      |

> **重要说明**：用户名、密码、端口号均为**目标远程设备**的信息，而非本系统的登录信息。例如，要连接一台 Windows 电脑，需填写该电脑的 Windows 登录用户名和密码。

填写完成后，点击 **"确认"** 即可添加新的远程连接。数据会持久化保存到后端数据库，重启服务后不会丢失。

> **提示**：如果通过无人机管理模块注册了无人机，系统会自动创建对应的远程设备，无需在此手动添加。自动创建的设备会继承无人机注册时填写的 IP 地址、端口、协议和用户名等信息。

#### 搜索连接

用户可在搜索表单中填写以下条件，点击 **"搜索"** 按钮进行筛选：

| 条件     | 说明                               |
| -------- | ---------------------------------- |
| 设备名称 | 按名称模糊匹配                     |
| IP地址   | 按 IP 地址模糊匹配                 |
| 端口号   | 按端口号精确匹配                   |
| 连接类型 | 按协议精确筛选（RDP/VNC/SSH/全部） |
| 用户名   | 按用户名模糊匹配                   |
| 描述     | 按描述信息模糊匹配                 |

#### 刷新连接

点击 **"刷新"** 按钮，系统会重新从后端加载远程连接列表，获取最新的设备状态和连接信息。

#### 断开所有连接

当用户需要断开所有已建立的远程连接时，点击 **"断开所有连接"** 按钮，系统会弹出确认窗口，确认后将断开所有在线连接，同时带宽使用进度条会归零。

> **注意**：断开连接后，设备将无法进行远程控制，需重新点击"连接"按钮恢复。

### 2.2 设备状态监测功能

#### 设备状态统计

页面展示了四个统计卡片，让用户直观了解当前远程连接的整体情况：

| 统计项   | 说明                                       |
| -------- | ------------------------------------------ |
| 在线设备 | 当前处于连接状态的设备数量                 |
| 离线设备 | 当前未连接的设备数量                       |
| 总连接数 | 系统中所有已添加的设备总数                 |
| 带宽使用 | 在线设备占总设备的百分比，反映当前网络负载 |

#### 设备状态显示

在远程连接列表中，每个设备都显示了其状态：

- **绿色圆点 + "在线"**：设备已连接，可进行远程操控
- **红色圆点 + "离线"**：设备未连接，需点击"连接"按钮建立连接

#### 带宽使用进度条

页面中部展示一个带宽使用进度条，以可视化方式展示当前在线设备占比。进度条会随设备连接/断开状态实时更新。

### 2.3 远程连接操作功能

#### 连接设备

对于列表中的离线设备，用户可点击 **“连接”** 按钮，系统会根据设备的连接类型自动选择对应的连接方式：

- **VNC 连接**：系统会在新标签页打开内置的 noVNC 客户端，通过 WebSocket 代理连接到目标设备的 VNC Server，在浏览器内直接操控远程桌面
- **SSH 连接**：系统会弹出密码输入框，确认后在新标签页打开内置的 SSH 终端（基于 xterm.js），通过 WebSocket 代理连接到目标设备的 SSH 服务，在浏览器内直接执行命令行操作
- **RDP 连接**：系统会自动生成一个 `.rdp` 连接文件并下载到本地，用户双击该文件即可通过 Windows 自带的远程桌面客户端（mstsc.exe）连接目标设备

> **前提条件**：目标设备必须已开机、运行对应的远程服务（VNC Server / RDP 服务 / SSH 服务），且与本机网络互通。

#### 断开设备连接

对于已连接的在线设备，操作列会显示 **"断开"** 按钮，点击后即可断开该设备的远程连接。

#### 删除设备

点击操作列的 **"删除"** 按钮，系统弹出确认窗口，确认后将从系统中移除该设备记录。

> **注意**：删除操作不可恢复，请谨慎操作。如果设备是通过无人机注册自动创建的，建议通过无人机管理模块删除无人机（会级联删除关联设备），而非在此单独删除设备记录。

### 2.4 连接统计功能

页面底部展示两个统计图表：

- **连接状态分布**（饼图）：以环形图形式展示在线和离线设备的占比情况，用户可直观了解当前远程连接的状态分布。
- **连接类型分布**（柱状图）：以水平柱状图形式展示不同连接类型（RDP、VNC、SSH）的使用数量，帮助用户了解各种连接协议的使用情况。

图表数据会随设备列表的变化实时更新。

### 2.5 使用提示功能

页面中的使用提示区域提供以下操作建议：

1. 请确保设备的IP地址和端口号正确，以保证连接成功。
2. 不同的连接类型可能需要不同的配置，请根据实际情况设置。
3. 断开连接后，设备将无法进行远程控制，需重新点击"连接"按钮恢复。

## 3. 技术实现

### 3.1 后端接口

| 接口                            | 方法      | 说明                                                                                                |
| ------------------------------- | --------- | --------------------------------------------------------------------------------------------------- |
| `/api/devices`                | GET       | 获取所有设备列表（支持 name、protocol 查询参数）                                                    |
| `/api/devices`                | POST      | 新增远程连接设备（JSON 格式）                                                                       |
| `/api/devices/:id`            | DELETE    | 删除指定 ID 的设备                                                                                  |
| `/api/devices/:id/status`     | POST      | 设置设备状态（online/offline）                                                                      |
| `/api/devices/disconnect-all` | POST      | 断开所有在线连接                                                                                    |
| `/api/vnc/ws`                 | WebSocket | VNC 代理（Query：`target=<IP>:<PORT>`），将浏览器 WebSocket 转发到目标设备的 VNC Server            |
| `/api/ssh/ws`                 | WebSocket | SSH 代理（Query：`host=<IP>&port=<PORT>&user=<USER>&pass=<PASS>`），将终端 I/O 通过 WebSocket 桥接到浏览器 |

### 3.2 数据存储

后端使用 SQLite 数据库的 `devices` 表存储设备信息，包含以下字段：

| 字段              | 类型     | 说明                       |
| ----------------- | -------- | -------------------------- |
| id                | INTEGER  | 主键，自增                 |
| name              | TEXT     | 设备名称                   |
| ip                | TEXT     | IP 地址                    |
| protocol          | TEXT     | 连接协议（VNC/RDP/SSH）    |
| port              | INTEGER  | 端口号                     |
| username          | TEXT     | 目标设备登录用户名         |
| password          | TEXT     | 目标设备登录密码           |
| auto_connect      | INTEGER  | 是否自动连接（0/1）        |
| log_enabled       | INTEGER  | 是否记录日志（0/1）        |
| description       | TEXT     | 描述信息                   |
| status            | TEXT     | 设备状态（online/offline） |
| created_at        | DATETIME | 创建时间                   |
| updated_at        | DATETIME | 更新时间                   |
| last_connected_at | DATETIME | 最后连接时间               |

### 3.3 前端技术

- 使用 Chart.js 绘制连接状态分布饼图和连接类型分布柱状图
- 使用模态弹窗实现新增连接和删除确认交互
- 搜索筛选在前端内存中执行，响应速度快
- 设备列表每 8 秒自动刷新，保持状态同步
- VNC 连接通过 noVNC 库实现浏览器内远程桌面
- SSH 连接通过 xterm.js 库实现浏览器内终端，支持终端窗口自适应和 256 色显示
- RDP 连接通过前端生成 `.rdp` 文件并触发下载，由系统自带客户端打开

### 3.4 连接协议说明

| 协议 | 默认端口 | 适用场景         | 目标设备要求                                  |
| ---- | -------- | ---------------- | --------------------------------------------- |
| VNC  | 5900     | 跨平台远程桌面   | 安装并启动 VNC Server（如 TightVNC、RealVNC） |
| RDP  | 3389     | Windows 远程桌面 | 开启 Windows 远程桌面功能                     |
| SSH  | 22       | Linux 终端控制   | 安装并启动 SSH 服务（如 OpenSSH）             |

### 3.5 三种连接方式的具体实现步骤

#### 3.5.1 VNC 连接实现步骤

**目标设备准备（被控端）：**

1. 在目标设备上安装 VNC Server 软件（推荐 TightVNC 或 RealVNC）
2. 启动 VNC Server，设置连接密码
3. 确保 VNC Server 监听端口（默认 5900）未被防火墙拦截
4. 确保目标设备与本机在同一局域网，或通过 VPN/端口转发互通

**本系统操作（控制端）：**

1. 在远程桌面控制页面点击 **“新增连接”**
2. 填写设备名称、目标设备 IP 地址、连接类型选择 **VNC**、端口号填写 **5900**（或自定义端口）
3. 填写用户名（可填任意标识，VNC 协议主要依赖密码认证）
4. 点击 **“确认”** 保存设备信息
5. 在设备列表中点击 **“连接”** 按钮
6. 系统自动在新标签页打开 noVNC 客户端页面（`vnc.html`）
7. noVNC 通过 WebSocket 连接到后端 `/api/vnc/ws?target=<被控端IP>:5900` 接口
8. 后端通过 `net.Dial("tcp", target)` 建立到目标设备 VNC Server 的 TCP 连接
9. 后端将 WebSocket 和 TCP 连接双向桥接，实现浏览器内远程桌面显示和操控

**技术流程图：**

```
浏览器(noVNC) ←WebSocket→ Go后端(VNCProxyWS) ←TCP→ 目标设备(VNC Server:5900)
```

---

#### 3.5.2 SSH 连接实现步骤

**目标设备准备（被控端）：**

1. 目标设备需为 Linux 系统（或其他支持 SSH 的系统）
2. 确保 SSH 服务已安装并启动（`sudo systemctl start sshd`）
3. 确保 SSH 端口（默认 22）未被防火墙拦截
4. 确保目标设备与本机网络互通

**本系统操作（控制端）：**

1. 在远程桌面控制页面点击 **“新增连接”**
2. 填写设备名称、目标设备 IP 地址、连接类型选择 **SSH**、端口号填写 **22**（或自定义端口）
3. 填写目标设备的 **Linux 系统用户名**（如 `root`、`ubuntu` 等）
4. 点击 **“确认”** 保存设备信息
5. 在设备列表中点击 **“连接”** 按钮
6. 系统弹出密码输入框，输入目标设备的 **Linux 系统密码**
7. 系统自动在新标签页打开 SSH 终端页面（`ssh.html`）
8. 前端 xterm.js 通过 WebSocket 连接到后端 `/api/ssh/ws?host=<被控端IP>&port=22&user=<用户名>&pass=<密码>` 接口
9. 后端使用 `golang.org/x/crypto/ssh` 库建立到目标设备的 SSH 连接
10. 后端请求 PTY（伪终端）并启动 Shell，将终端的 stdin/stdout/stderr 与 WebSocket 双向桥接
11. 用户在浏览器终端中输入的命令通过 WebSocket 发送到后端，后端转发到 SSH 会话的 stdin
12. 目标设备的命令输出通过 SSH 会话的 stdout 返回后端，后端通过 WebSocket 推送到浏览器终端显示

**技术流程图：**

```
浏览器(xterm.js) ←WebSocket→ Go后端(SSHProxyWS) ←SSH协议→ 目标设备(SSH Server:22)
                                        │
                              golang.org/x/crypto/ssh
                              │─ ssh.Dial() 建立连接
                              │─ session.RequestPty() 请求伪终端
                              │─ session.Shell() 启动 Shell
                              └─ stdin/stdout/stderr 双向桥接
```

**SSH 终端特性：**

- 支持 xterm-256color 终端类型，完整显示彩色输出
- 支持终端窗口自动调整大小（通过 `\x01` + JSON 消息发送 resize 指令）
- 支持光标闪烁、滚动、选择复制等标准终端操作
- 连接状态实时显示（连接中/已连接/已断开）

---

#### 3.5.3 RDP 连接实现步骤

**目标设备准备（被控端）：**

1. 目标设备需为 Windows 系统（Windows 10/11 专业版或企业版）
2. 开启远程桌面功能：设置 → 系统 → 远程桌面 → 启用远程桌面
3. 确保 RDP 端口（默认 3389）未被防火墙拦截
4. 确保目标设备与本机网络互通

**本系统操作（控制端）：**

1. 在远程桌面控制页面点击 **“新增连接”**
2. 填写设备名称、目标设备 IP 地址、连接类型选择 **RDP**、端口号填写 **3389**（或自定义端口）
3. 填写目标设备的 **Windows 登录用户名**（如 `Administrator`、`user` 等）
4. 点击 **“确认”** 保存设备信息
5. 在设备列表中点击 **“连接”** 按钮
6. 系统自动生成一个 `.rdp` 连接文件，包含以下配置信息：
   - `full address` — 目标设备 IP 和端口
   - `username` — 登录用户名
   - `screen mode id` — 全屏模式
   - `desktopwidth/desktopheight` — 分辨率 1920×1080
   - `session bpp` — 32 位色彩
   - `prompt for credentials` — 提示输入密码
7. 浏览器自动下载该 `.rdp` 文件
8. 用户双击下载的 `.rdp` 文件，Windows 系统自动调用 `mstsc.exe`（远程桌面连接客户端）打开
9. 在弹出的 Windows 远程桌面窗口中输入目标设备的 Windows 密码，即可连接

**技术流程图：**

```
本系统前端 → 生成 .rdp 文件 → 浏览器下载 → 用户双击 → mstsc.exe ←RDP协议→ 目标设备(RDP:3389)
```

> **说明**：RDP 是微软的私有协议，没有成熟的纯浏览器实现方案，因此采用生成 `.rdp` 文件 + 系统自带客户端的方式，这是业界通用做法，兼容性最好。

---

#### 3.5.4 三种连接方式对比

| 特性               | VNC                 | SSH                | RDP                      |
| ------------------ | ------------------- | ------------------ | ------------------------ |
| 是否在浏览器内完成 | ✅ 是               | ✅ 是              | ❌ 否（需系统客户端）    |
| 操控方式           | 图形化桌面          | 命令行终端         | 图形化桌面               |
| 后端代理           | TCP 转发            | SSH 库连接         | 无（前端生成文件）       |
| 前端库             | noVNC               | xterm.js           | 无（系统自带 mstsc.exe） |
| 适用系统           | 跨平台              | Linux/macOS        | Windows                  |
| 默认端口           | 5900                | 22                 | 3389                     |
| 带宽消耗           | 高（图形传输）      | 低（纯文本）       | 中高（图形传输，有压缩） |
| 密码输入时机       | VNC Server 内部认证 | 点击连接时弹出输入 | 打开 .rdp 文件后输入     |

## 4. 使用指南

1. **添加设备**：点击“新增连接”按钮，填写目标设备的名称、IP、端口、用户名等信息后点击“确认”。
2. **连接 VNC 设备**：点击“连接”按钮，系统自动在新标签页打开远程桌面，直接在浏览器内操控。
3. **连接 SSH 设备**：点击“连接”按钮，输入密码后系统在新标签页打开终端，可直接执行 Linux 命令。
4. **连接 RDP 设备**：点击“连接”按钮，系统自动下载 `.rdp` 文件，双击即可用 Windows 远程桌面连接。
5. **断开连接**：点击在线设备的“断开”按钮，或点击“断开所有连接”一键断开全部。
6. **搜索设备**：在搜索表单中输入条件，点击“搜索”按钮快速定位设备。
7. **删除设备**：点击“删除”按钮，在确认弹窗中确认后移除设备。
8. **查看统计**：页面底部的图表实时展示连接状态和类型分布。

## 5. 测试环境要求

| 测试场景           | 所需环境                                               |
| ------------------ | ------------------------------------------------------ |
| 添加/搜索/删除设备 | 1 台电脑（运行后端 + 浏览器）                          |
| VNC 远程连接       | 2 台电脑（后端机 + 目标机安装 VNC Server，需网络互通） |
| RDP 远程连接       | 2 台电脑（后端机 + 目标 Windows 机开启远程桌面）       |
| SSH 远程连接       | 2 台电脑（后端机 + 目标 Linux 机开启 SSH 服务）        |

## 6. 相关文件

| 文件路径                             | 说明                                                                                                                      |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| `project/web/modules/remote.html`  | 前端页面（HTML + CSS + JavaScript）                                                                                       |
| `project/web/modules/common.js`    | 公共工具函数（API 请求、图表创建、消息提示）                                                                              |
| `project/web/modules/common.css`   | 公共样式                                                                                                                  |
| `project/web/vnc.html`             | VNC 远程桌面客户端页面（基于 noVNC）                                                                                      |
| `project/web/ssh.html`             | SSH 终端客户端页面（基于 xterm.js）                                                                                       |
| `project/internal/handlers/api.go` | 后端 API 处理（DevicesList、DevicesCreate、DevicesDelete、DeviceSetStatus、DevicesDisconnectAll、VNCProxyWS、SSHProxyWS） |
| `project/internal/db/db.go`        | 数据库表结构定义（devices 表）                                                                                            |

## 7. 观察端和被控端的配置方法

 > 目标：让未配置的观察端与被控端（全新笔记本/无人机控制终端）完成对本系统远程控制功能的测试与连接。  
 > 支持协议：VNC（跨平台图形桌面）、SSH（Linux 终端）、RDP（Windows 远程桌面）  
 > 架构：浏览器 → 本系统后端（WebSocket 代理）→ 目标被控端

 > **新版本说明**：当前版本支持通过 **无人机管理模块** 注册无人机时自动创建关联的远程设备，无需手动添加。也可通过远程桌面控制页面手动新增独立设备。

 ---
 
 ### 目录
 
 - [角色定义](#角色定义)
 - [网络拓扑与 IP 规划](#网络拓扑与-ip-规划)
 - [后端主机（电脑A）部署与启动](#后端主机电脑a部署与启动)
 - [被控端配置—— VNC](#被控端配置-vnc)
 - [被控端配置—— SSH（Linux）](#被控端配置-sshlinux)
 - [被控端配置—— RDP（Windows）](#被控端配置-rdpwindows)
 - [观察端配置（浏览器）](#观察端配置浏览器)
 - [连通性验证流程](#连通性验证流程)
 - [常见报错与排查](#常见报错与排查)
 - [快速验证流程](#快速验证流程)
 - [附录：关键命令与端口](#附录关键命令与端口)
 
 ---
 
 ### 角色定义
 
 | 角色 | 设备 | 主要职责 | 所需配置 |
 |------|------|----------|----------|
 | **后端主机** | 运行本系统的服务器 | 提供 Web 界面与 VNC/SSH WebSocket 代理 | 启动本系统，确保 8080 端口可访问 |
 | **被控端** | 目标主机/无人机控制终端 | 运行远程服务，接受远程控制 | 根据协议安装对应服务（VNC/SSH/RDP）并开放端口 |
 | **观察端** | 测试人员浏览器 | 通过 Web 界面连接并控制被控端 | 访问系统页面，输入目标地址进行连接 |
 
 ---
 
 ### 网络拓扑与 IP 规划
 
 ```text
 观察端浏览器 ──(HTTP/WS)──> 后端主机 :8080 ──(TCP)──> 被控端 :5900/22/3389
 ```
 
 - **后端主机**：`192.168.1.10`（运行本系统）
 - **被控端**：`192.168.1.20`（目标主机/无人机控制终端）
 - **观察端**：任意可访问后端的浏览器
 
 **关键要求**：
 
 - 后端主机必须能直连被控端的对应端口（VNC: 5900、SSH: 22、RDP: 3389）
 - 观察端只需访问后端的 8080 端口
 - RDP 连接时，观察端需能直达被控端的 3389 端口（不经过后端代理）
 
 ---
 
 ### 后端主机（电脑A）部署与启动
 
 > 说明：后端主机是运行本系统的电脑/服务器，它负责提供 Web 界面（HTTP 8080）以及 VNC/SSH WebSocket 代理（`/api/vnc/ws`、`/api/ssh/ws`）。
 
 #### 1. 获取项目代码
 
 - **方式 A：复制文件夹**
   - 将整个项目文件夹复制到电脑A。
 
 - **方式 B：Git 克隆（如有仓库）**
   - 在电脑A上 `git clone <仓库地址>`。
 
 #### 2. 进入后端项目目录
 
 在电脑A上打开终端并进入 `project` 目录（该目录内有 `main.go` 与 `go.mod`）：
 
 ```bash
 cd project
 ```
 
 #### 3. 安装依赖
 
 ```bash
 go mod tidy
 ```
 
 #### 4. 启动服务
 
 ```bash
 go run .
 ```
 
 #### 5. 确认启动成功
 
 看到以下输出表示成功：
 
 ```text
 listening on :8080
 ```
 
 #### 6. 验证后端可访问
 
 在电脑A本机浏览器访问：
 
 - `http://127.0.0.1:8080/api/healthz`
 
 应返回：
 
 ```json
 {"status":"ok"}
 ```
 
 如果观察端在另一台机器上，改用：
 
 - `http://<电脑A的IP>:8080/api/healthz`
 
 并确保电脑A的防火墙允许入站访问 8080 端口（或你实际配置的监听端口）。
 
 ### 被控端配置—— VNC
 
 > 适用于跨平台图形化远程桌面控制场景。如果被控端是 Linux 终端，推荐使用 SSH；如果是 Windows，也可使用 RDP。
 
 #### 被控端 VNC 配置（全新笔记本）
 
 #### 1. 安装 VNC 服务
 
 ##### Windows（推荐 TightVNC）
 
 1. **下载安装**
 
    ```powershell
    # 访问 https://www.tightvnc.com/download.php 下载最新版
    # 安装时勾选 Server 和 Viewer，建议安装为服务
    ```
 
 2. **设置密码**
    - 打开 "TightVNC Server" 配置界面
    - 设置 "Primary password"（控制密码）
    - 可选设置 "View-only password"（只读密码）
    - 勾选 "Allow control of this computer via password"
 
 3. **启动服务**
    - 确保 "TightVNC Server" 服务状态为 "Running"
    - 建议设置为开机自启动
 
 ##### Linux（Ubuntu/Debian 示例）
 
 ```bash
 sudo apt update
 sudo apt install -y tigervnc-standalone-server
 vncpasswd  # 设置 VNC 密码
 vncserver :1 -geometry 1920x1080 -depth 24  # 启动 VNC 服务
 ```
 
 ##### macOS
 
 1. 系统设置 → 共享 → 屏幕共享
 2. 勾选 "屏幕共享"
 3. 点击 "电脑设置" 设置 VNC 密码
 
 #### 2. 防火墙配置
 
 ##### Windows 防火墙（以管理员身份运行 PowerShell）
 
 ```powershell
 # 允许 VNC 5900 端口入站
 New-NetFirewallRule -DisplayName "VNC 5900" -Direction Inbound -Protocol TCP -LocalPort 5900 -Action Allow
 
 # 验证规则是否生效
 Get-NetFirewallRule -DisplayName "VNC 5900"
 ```
 
 ##### Linux 防火墙（ufw 示例）
 
 ```bash
 sudo ufw allow 5900/tcp
 sudo ufw reload
 ```
 
 ##### macOS 防火墙
 
 1. 系统偏好设置 → 安全性与隐私 → 防火墙
 2. 点击 "防火墙选项"
 3. 添加 "屏幕共享" 或允许传入连接
 
 #### 3. 第三方安全软件配置
 
 - **杀毒软件**：临时关闭或添加 5900 端口例外
 - **企业防护**：在安全策略中放行 VNC 连接
 - **VPN/代理**：确保不会拦截本地网络连接
 
 #### 4. 验证 VNC 服务
 
 ```powershell
 # Windows
 netstat -ano | findstr :5900
 
 # Linux/macOS
 ss -lnt | grep 5900
 ```
 
 应看到类似 `TCP 0.0.0.0:5900` 的监听状态。
 
 ---
 
 ### 被控端配置—— SSH（Linux）
 
 > 适用于 Linux/macOS 系统的命令行终端远程控制场景。系统通过浏览器内 xterm.js 终端实现 SSH 连接。
 
 #### 1. 确认 SSH 服务已安装并启动
 
 ##### Ubuntu/Debian
 
 ```bash
 sudo apt update
 sudo apt install -y openssh-server
 sudo systemctl enable ssh
 sudo systemctl start ssh
 ```
 
 ##### CentOS/RHEL
 
 ```bash
 sudo yum install -y openssh-server
 sudo systemctl enable sshd
 sudo systemctl start sshd
 ```
 
 ##### macOS
 
 macOS 自带 SSH 服务，只需在"系统设置 → 通用 → 共享 → 远程登录"中开启即可。
 
 #### 2. SSH 防火墙配置
 
 ##### Linux（ufw 示例）
 
 ```bash
 sudo ufw allow 22/tcp
 sudo ufw reload
 ```
 
 ##### Linux（firewalld 示例）
 
 ```bash
 sudo firewall-cmd --permanent --add-service=ssh
 sudo firewall-cmd --reload
 ```
 
 #### 3. 验证 SSH 服务状态
 
 ```bash
 ss -lnt | grep 22
 ```
 
 应看到类似 `0.0.0.0:22` 的监听状态。
 
 #### 4. 在本系统中添加 SSH 设备
 
 1. 登录系统，进入远程桌面控制模块
 2. 新增 SSH 设备，点击连接，输入密码
 
 > **提示**：如果已通过无人机管理模块注册了无人机并选择 SSH 协议，系统会自动创建对应的远程设备，无需手动添加。
 
 ---
 
 ### 被控端配置—— RDP（Windows）
 
 > 适用于 Windows 系统的图形化远程桌面控制场景。系统生成 `.rdp` 文件，由本地 Windows 远程桌面客户端（mstsc.exe）打开。
 
 #### 1. 开启 Windows 远程桌面
 
 1. 打开"设置 → 系统 → 远程桌面"
 2. 开启"启用远程桌面"开关
 3. 记录当前电脑名称（可选，用于连接标识）
 
 > **注意**：Windows 10/11 **家庭版**不支持 RDP 服务端，需使用**专业版**或**企业版**。
 
 #### 2. RDP 防火墙配置
 
 ```powershell
 # 允许 RDP 3389 端口入站（以管理员身份运行 PowerShell）
 New-NetFirewallRule -DisplayName "RDP 3389" -Direction Inbound -Protocol TCP -LocalPort 3389 -Action Allow
 
 # 验证规则是否生效
 Get-NetFirewallRule -DisplayName "RDP 3389"
 ```
 
 #### 3. 验证 RDP 服务状态
 
 ```powershell
 netstat -ano | findstr :3389
 ```
 
 应看到类似 `TCP 0.0.0.0:3389` 的监听状态。
 
 #### 4. 在本系统中添加 RDP 设备
 
 1. 登录系统，进入远程桌面控制模块
 2. 新增 RDP 设备，点击连接，双击下载的 `.rdp` 文件
 
 > **提示**：如果已通过无人机管理模块注册了无人机并选择 RDP 协议，系统会自动创建对应的远程设备，无需手动添加。
 
 ---
 
 ### 观察端配置（浏览器）
 
 #### 1. 基本要求
 
 - **浏览器**：Chrome、Firefox、Edge 等现代浏览器
 - **网络**：能访问后端主机的 8080 端口
 - **插件**：无需额外插件（使用 HTML5 WebSocket）
 
 #### 2. 访问方式
 
 ##### 方式一：VNC 直接连接页（推荐快速测试）
 
 1. 打开浏览器访问：`http://<后端IP>:8080/app/vnc.html`
 2. 在目标地址输入框中填入：`<被控端IP>:5900`
 3. 点击 "连接" 按钮
 4. 如有密码弹窗，输入被控端设置的 VNC 密码
 
 ##### 方式二：通过设备管理页（推荐正式使用）
 
 1. 访问：`http://<后端IP>:8080/app/login.html` 登录系统
 2. 进入 "远程桌面控制" 模块
 3. 点击 "新增连接"，填写设备信息：
    - 设备名称：自定义（如 "测试笔记本"、"无人机-01"）
    - IP 地址：被控端 IP
    - 连接类型：选择 **VNC** / **SSH** / **RDP**
    - 端口号：VNC 默认 5900、SSH 默认 22、RDP 默认 3389
    - 用户名：目标设备的系统用户名
 4. 在设备列表中点击 "连接"：
    - **VNC**：新标签页打开 noVNC 远程桌面
    - **SSH**：弹出密码框后新标签页打开 xterm.js 终端
    - **RDP**：自动下载 `.rdp` 文件，双击用 mstsc.exe 打开
 
 ##### 方式三：通过无人机管理自动创建（推荐无人机场景）
 
 1. 进入 "无人机管理" 模块，注册无人机时填写 IP、端口、协议等信息
 2. 系统自动在 "远程桌面控制" 模块创建对应设备
 3. 直接在设备列表中点击 "连接" 即可
 
 #### 3. 连接状态说明
 
 | 状态 | 含义 | 操作建议 |
 |------|------|----------|
 | 未连接 | 初始状态 | 检查目标地址格式 |
 | 正在连接 | WebSocket 建立中 | 等待或检查网络 |
 | ✓ 已连接 | 连接成功 | 可开始远程控制 |
 | ✗ 已断开 | 连接中断 | 检查网络或远程服务 |
 
 #### 4. 功能说明
 
 - **VNC**：鼠标键盘完全控制被控端桌面，自动适应浏览器窗口大小
 - **SSH**：浏览器内终端，支持 256 色显示、窗口自适应、光标闪烁等
 - **RDP**：通过系统自带客户端连接，支持全屏、高分辨率
 - **断开连接**：点击 "断开" 按钮或关闭页面
 
 ---
 
 ### 连通性验证流程
 
 #### 1. 后端 → 被控端验证
 
 在后端主机上执行以下命令，根据使用的协议选择对应端口：
 
 ##### Windows 后端
 
 ```powershell
 # VNC
 Test-NetConnection 192.168.1.20 -Port 5900
 # SSH
 Test-NetConnection 192.168.1.20 -Port 22
 # RDP
 Test-NetConnection 192.168.1.20 -Port 3389
 ```
 
 成功应显示：`TcpTestSucceeded : True`
 
 ##### Linux/macOS 后端
 
 ```bash
 # VNC
 nc -vz 192.168.1.20 5900
 # SSH
 nc -vz 192.168.1.20 22
 # RDP
 nc -vz 192.168.1.20 3389
 ```
 
 成功应显示连接成功信息。
 
 #### 2. 观察端 → 后端验证
 
 在观察端浏览器中：
 
 1. 访问：`http://<后端IP>:8080/api/healthz`
 2. 应返回：`{"status":"ok"}`
 
 #### 3. 完整链路测试
 
 ##### VNC 链路
 
 1. 观察端访问 `/app/vnc.html`
 2. 输入 `<被控端IP>:5900`
 3. 打开浏览器开发者工具，查看 Network 标签
 4. 应看到 WebSocket 连接到 `/api/vnc/ws`，状态为 101 Switching Protocols
 
 ##### SSH 链路
 
 1. 在设备管理页添加 SSH 设备并点击 "连接"
 2. 输入密码后应在新标签页打开终端
 3. Network 标签应看到 WebSocket 连接到 `/api/ssh/ws`
 
 ##### RDP 链路
 
 1. 在设备管理页添加 RDP 设备并点击 "连接"
 2. 应自动下载 `.rdp` 文件
 3. 双击文件后应弹出 Windows 远程桌面连接窗口
 
 ---
 
 ### 常见报错与排查
 
 | 错误现象 | 可能原因 | 解决方案 |
 |----------|----------|----------|
 | **VNC 连接失败/502 错误** | 后端无法直连被控端 5900 | 在后端执行 `Test-NetConnection` 检查连通性 |
 | **VNC 安全验证失败** | VNC 密码错误或不兼容 | 重新设置 VNC 密码，使用标准 VNC 软件 |
 | **SSH 认证失败** | 用户名或密码错误 | 确认使用被控端的系统用户名和密码 |
 | **SSH 连接被拒绝** | SSH 服务未启动或端口未开放 | 检查 `sshd` 服务状态和防火墙规则 |
 | **RDP 文件下载后无法连接** | 被控端未开启远程桌面或防火墙拦截 | 检查 Windows 远程桌面设置和 3389 端口 |
 | **RDP 提示"家庭版不支持"** | Windows 家庭版无 RDP 服务端 | 升级到专业版/企业版，或改用 VNC |
 | **连接超时** | 防火墙或杀毒软件拦截 | 检查防火墙规则，临时关闭安全软件 |
 | **noVNC 库加载失败** | CDN 被拦截或网络问题 | 换网络环境或使用 `/app/vnc_simple.html` |
 | **一直"正在连接..."** | 网络丢包或远程服务异常 | 重启对应服务，检查网络质量 |
 | **端口被占用** | 其他程序占用对应端口 | 使用 `netstat` 查找占用进程并停止 |
 
 ---
 
 ### 快速验证流程（推荐顺序）
 
 #### 步骤 1：被控端配置（根据协议选择）
 
 **VNC 被控端（Windows）：**
 
 ```powershell
 # 1. 安装 TightVNC（下载安装包）
 # 2. 设置 VNC 密码
 # 3. 启动 VNC 服务
 # 4. 开放防火墙
 New-NetFirewallRule -DisplayName "VNC 5900" -Direction Inbound -Protocol TCP -LocalPort 5900 -Action Allow
 # 5. 验证监听
 netstat -ano | findstr :5900
 ```
 
 **SSH 被控端（Linux）：**
 
 ```bash
 sudo apt install -y openssh-server && sudo systemctl enable ssh && sudo systemctl start ssh
 sudo ufw allow 22/tcp && sudo ufw reload
 ss -lnt | grep 22
 ```
 
 **RDP 被控端（Windows）：**
 
 ```powershell
 # 1. 设置 → 系统 → 远程桌面 → 启用远程桌面
 # 2. 开放防火墙
 New-NetFirewallRule -DisplayName "RDP 3389" -Direction Inbound -Protocol TCP -LocalPort 3389 -Action Allow
 # 3. 验证监听
 netstat -ano | findstr :3389
 ```
 
 #### 步骤 2：后端验证
 
 ```powershell
 # 检查到被控端的连通性（根据协议选择端口）
 Test-NetConnection <IP> -Port 5900   # VNC
 Test-NetConnection <IP> -Port 22     # SSH
 Test-NetConnection <IP> -Port 3389   # RDP
 ```
 
 #### 步骤 3：观察端连接
 
 **VNC：**
 
 1. 访问：`http://192.168.1.10:8080/app/vnc.html`
 2. 输入：`192.168.1.20:5900`，点击连接，输入密码
 
 **SSH：**
 
 1. 登录系统，进入远程桌面控制模块
 2. 新增 SSH 设备，点击连接，输入密码
 
 **RDP：**
 
 1. 登录系统，进入远程桌面控制模块
 2. 新增 RDP 设备，点击连接，双击下载的 `.rdp` 文件
 
 ---
 
 ### 附录：关键命令与端口
 
 #### 端口说明
 
 | 端口 | 用途 | 说明 |
 |------|------|------|
 | 8080 | 后端 Web 服务 | 观察端访问界面 |
 | 5900 | VNC 服务 | 被控端监听端口（跨平台图形桌面） |
 | 22 | SSH 服务 | 被控端监听端口（Linux 终端） |
 | 3389 | RDP 服务 | 被控端监听端口（Windows 远程桌面） |
 | 9100 | hw-agent | Agent 数据采集服务（可选） |
 
 #### 关键端点
 
 - **健康检查**：`http://<后端IP>:8080/api/healthz`
 - **VNC 直连页**：`http://<后端IP>:8080/app/vnc.html`
 - **设备管理页**：`http://<后端IP>:8080/app/modules/remote.html`
 - **无人机管理页**：`http://<后端IP>:8080/app/modules/drones.html`
 - **VNC WebSocket 代理**：`ws://<后端IP>:8080/api/vnc/ws?target=<IP>:5900`
 - **SSH WebSocket 代理**：`ws://<后端IP>:8080/api/ssh/ws`
 - **电池实时事件流**：`ws://<后端IP>:8080/api/battery/stream`
 - **飞行任务实时事件流**：`ws://<后端IP>:8080/api/flight/stream`
 
 #### 常用命令
 
 ```powershell
 # Windows 检查端口监听
 netstat -ano | findstr :5900   # VNC
 netstat -ano | findstr :3389   # RDP
 
 # Windows 检查防火墙规则
 Get-NetFirewallRule -DisplayName "VNC 5900"
 Get-NetFirewallRule -DisplayName "RDP 3389"
 
 # Windows 测试连通性
 Test-NetConnection <IP> -Port 5900   # VNC
 Test-NetConnection <IP> -Port 22     # SSH
 Test-NetConnection <IP> -Port 3389   # RDP
 
 # Linux/macOS 检查端口监听
 ss -lnt | grep 5900   # VNC
 ss -lnt | grep 22     # SSH
 
 # Linux/macOS 测试连通性
 nc -vz <IP> 5900   # VNC
 nc -vz <IP> 22     # SSH
 nc -vz <IP> 3389   # RDP
 ```
 
 ---
 
 ### 一键检查脚本（被控端 Windows）
 
 以管理员身份在 PowerShell 中执行：
 
 ```powershell
 Write-Host "=== 远程服务配置检查 ===" -ForegroundColor Green
 
 # 检查 VNC 服务
 Write-Host "1. 检查 VNC 服务状态..."
 Get-Service | Where-Object {$_.Name -like "*tightvnc*" -or $_.DisplayName -like "*vnc*"} | Format-Table Name, Status, DisplayName
 
 # 检查端口监听
 Write-Host "2. 检查关键端口监听..."
 Write-Host "  VNC (5900):"
 netstat -ano | findstr :5900
 Write-Host "  RDP (3389):"
 netstat -ano | findstr :3389
 
 # 检查防火墙规则
 Write-Host "3. 检查防火墙规则..."
 Get-NetFirewallRule -DisplayName "VNC 5900" -ErrorAction SilentlyContinue | Format-Table DisplayName, Enabled, Direction
 Get-NetFirewallRule -DisplayName "RDP 3389" -ErrorAction SilentlyContinue | Format-Table DisplayName, Enabled, Direction
 
 # 测试本地连接
 Write-Host "4. 测试本地连接..."
 Test-NetConnection 127.0.0.1 -Port 5900
 Test-NetConnection 127.0.0.1 -Port 3389
 
 Write-Host "=== 检查完成 ===" -ForegroundColor Green
 ```
 
 ---
 
 ### 故障排查清单
 
 如果连接失败，请按以下顺序检查：
 
 1. **被控端**
    - [ ] 对应远程服务已启动（VNC Server / SSH 服务 / Windows 远程桌面）
    - [ ] 防火墙已开放对应端口（VNC: 5900、SSH: 22、RDP: 3389）
    - [ ] 第三方安全软件已放行
    - [ ] IP 地址正确
 
 2. **后端主机**
    - [ ] 能访问被控端的对应端口
    - [ ] 本系统服务正常运行
    - [ ] 8080 端口可访问
 
 3. **观察端**
    - [ ] 浏览器能访问后端 8080 端口
    - [ ] 输入的目标地址和端口格式正确
    - [ ] 网络连接稳定
    - [ ] RDP 连接时能直达被控端 3389 端口
 
 4. **网络环境**
    - [ ] 同一局域网或路由配置正确
    - [ ] 无 VPN/代理干扰
    - [ ] 带宽充足
 
 ---
 
 > **提示**：完成配置后，建议保存各设备的 IP 地址、端口号和登录凭据，便于后续测试使用。如果使用无人机管理模块注册无人机，系统会自动创建关联的远程设备，简化配置流程。
