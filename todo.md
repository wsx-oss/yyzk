# CloudControl 项目优化 To Do

> 本文档按**事项完成顺序**编排，不按天数拆分，不按每日工作量平均分配。  
> 目标是围绕当前项目现状，按“先打基础、再扩能力、最后做展示”的顺序推进优化。

## 一、项目当前基础判断

- 当前后端为 Go + Gin 单体服务，数据库已迁移至 MySQL（Aiven 云端），核心数据逻辑集中在 `project/internal/db/db.go` 与多个 handler SQL 中，内置 `AdaptSQL` 自动适配 SQL 方言。
- 当前前端为 `dashboard.html + iframe + 多模块 HTML` 结构，适合做增量改造，但不适合直接大规模重写。
- 当前已有 AI 能力主要集中在航线规划和 CoT 分析，还没有统一的右下角 AI 助手入口。
- 当前已有告警、日志、电池、GPS、任务数据，但没有统一的右上角消息提醒中心。
- 当前模拟能力只有单机 demo 级别的 `provider_simulated.go`，还不具备几十台模拟机、7x24 小时、强化学习闭环能力。
- 当前仓库可编译运行，但自动化测试为空，后续改造需要同步补最小测试与验证链路。

---

## 二、整体完成顺序

1. 数据库迁移：SQLite 改为 MySQL
2. 数据备份与数据回滚
3. 右下角 AI 小助手 + 右上角消息通知小喇叭
4. 无人机任务分类与调度优化
5. 后端线程 / 进程并发能力建设
6. 后端线程 / 进程创建与应用优化落地
7. 模拟无人机群
8. 模拟数据统计与动态可视化
9. 前端整体风格优化

---

## 三、详细 To Do

### 阶段 1：数据库迁移（SQLite -> MySQL）

目标：先把项目的数据底座从本地 SQLite 升级为 MySQL，为后续模拟机集群、并发处理、备份回滚、统计分析打基础。

- [x] 梳理当前所有表结构、索引、默认值、时间字段、外键关系。
- [x] 识别所有 SQLite 专用语法并整理迁移清单。
  - `PRAGMA`
  - `journal_mode=WAL`
  - `datetime('now')` → `NOW()` （由 `AdaptSQL` 自动转换）
  - `INSERT OR REPLACE` → `REPLACE INTO`
  - SQLite 自增主键 → MySQL `AUTO_INCREMENT`
- [x] 重构数据库初始化层，支持 MySQL 连接配置。
  - 新增 MySQL DSN 配置（`.env`）
  - 新增连接池配置（25 连接 / 10 空闲）
  - 支持通过环境变量 `SC_DB_DRIVER` 切换数据库
- [x] 将 `project/internal/db/db.go` 改造成可扩展的数据源初始化入口。
- [x] 把核心表迁移到 MySQL（26 张表全部完成）。
  - 用户与登录会话
  - 无人机与设备信息
  - GPS 数据
  - 电池数据
  - 飞行任务
  - 告警日志
  - CoT / AI 记录
  - 同步任务与性能报告
- [x] 编写 SQLite -> MySQL 数据迁移脚本（`migrate_sqlite_to_mysql.py`）。
- [x] 完成一次全量迁移验证，1473 行数据全部导入新库。
- [x] 验证 MySQL 下项目可正常启动、注册、登录、增删改查。

阶段输出：

- [x] MySQL 版本数据库可独立运行
- [x] 旧 SQLite 数据可迁移（已完成）
- [x] 后端主流程切换到 MySQL
- [x] 系统时区统一为 Asia/Shanghai (UTC+8)

---

### 阶段 2：备份与数据回滚

目标：在 MySQL 基础上补齐自动备份、手动备份、数据恢复、数据回滚能力，保证后续大规模仿真与并发改造有兜底。

- [x] 设计备份策略。
  - 自动备份
  - 手动备份
  - 备份保留周期
  - 备份文件命名规范
- [x] 实现自动备份功能。
  - 按时间周期自动执行（24 小时周期 + 启动后首次备份）
  - 自动记录备份结果（backup_records 表）
  - 失败自动告警（写入 notifications 表）
- [x] 实现手动备份接口与前端操作入口。（`POST /api/backup/manual` + `backup.html`）
- [x] 设计备份记录表。（`backup_records`）
  - 备份时间（created_at / finished_at）
  - 备份类型（auto / manual / pre_restore）
  - 文件路径（file_path）
  - 状态（running / success / failed）
  - 操作人（operator）
- [x] 实现数据恢复与回滚功能。（`POST /api/backup/restore/:id`）
  - 从指定备份恢复
  - 按备份时间点恢复（选择对应备份 ID）
  - 手动触发回滚（前端一键恢复按钮）
- [x] 增加恢复前安全检查机制。
  - 当前库状态检查（备份状态必须为 success）
  - 备份文件有效性检查（文件是否存在）
  - 恢复前确认记录（自动创建 pre_restore 备份）
- [x] 在前端增加备份管理模块或集成入口。（`backup.html`）
  - 备份列表（含状态、大小、行数、耗时等）
  - 立即备份（手动触发按钮）
  - 一键回滚（恢复按钮 + 确认弹窗）
  - 恢复日志（通知中心集成）
- [x] 完成一次完整的备份与恢复演练。（启动后自动执行首次备份验证）
- [x] 修复备份模块前端 `confirmAction()` 逻辑 Bug。
  - `closeModal()` 在执行前将 `pendingAction` 置空，导致所有模态框确认操作（删除、恢复、清理）全部失效
  - 修复方法：先保存 `pendingAction` 引用再调用 `closeModal()`
- [x] 优化备份模块用户交互体验。
  - 数据恢复操作增加即时 Toast 反馈（「正在发起数据恢复请求...」→「数据恢复已开始」）
  - 文件上传恢复增加即时 Toast 反馈（「正在上传文件并发起恢复...」→「数据恢复已开始」）
  - 删除操作增加即时 Toast 反馈（「正在删除...」）
  - 恢复成功后自动弹出进度弹窗，实时显示恢复阶段与进度

阶段输出：

- [x] 具备自动备份能力（`StartAutoBackup` 24h 周期 + 启动首次备份）
- [x] 具备手动备份能力（`/api/backup/manual` + 前端按钮）
- [x] 具备数据恢复与回滚能力（`/api/backup/restore/:id` + 前端恢复按钮）
- [x] 前端模态框确认操作正常（删除 / 恢复 / 清理均已验证）
- [x] 用户操作具备即时反馈（Toast + 进度弹窗）

---

### 阶段 3：AI 小助手 + 消息通知小喇叭

目标：完成平台 AI 交互入口和统一通知入口，增强“能问、能查、能提醒、能跳转”的智能操作体验。

#### 3.1 右下角 AI 小助手

- [x] 在平台主界面右下角新增 AI 助手浮窗入口。（`ai-assistant.js`）
- [x] 实现 AI 助手基础会话面板。
  - 欢迎语
  - 会话历史
  - 输入框
  - 快捷指令按钮
- [x] 接入当前已有 AI 能力。
  - 飞行规划解释
  - 告警分析
  - 电池状态解读
  - CoT 决策结果调用
- [x] 扩展 AI 助手能力定位。
  - 操作引导
  - 任务咨询
  - 快捷指令
  - 问题解答
  - 数据查询
- [x] 建立 AI 助手知识源。
  - 功能介绍文档
  - 当前项目业务数据
  - 告警记录
  - 日志数据
  - 模拟机统计数据
- [x] 支持 AI 助手跳转到相关模块页面。
  - 跳转任务页
  - 跳转告警页
  - 跳转模拟机页
  - 跳转电池页

#### 3.2 右上角消息通知小喇叭

- [x] 在平台右上角增加消息提醒图标（小喇叭 / 铃铛）。（`notification-bell.js`）
- [x] 建立统一通知中心。
  - 未读数量
  - 通知列表
  - 时间排序
  - 类型筛选
  - 点击跳转
- [x] 接入系统通知来源。（`notification.go`）
  - 无人机异常
  - 电池低电量 / 温度异常
  - 任务完成
  - 系统告警
  - 后端日志异常
  - 备份失败
  - 模拟机异常
- [x] 增加 AI 助手定时巡检逻辑。（`patrol.go`）
  - 定时检查模拟机飞行情况
  - 定时检查后端日志
  - 定时检查电池等设备安全性
  - 发现问题后生成通知
- [x] 支持通知已读 / 全部已读 / 保留最近通知。
- [x] 建立通知事件表和推送接口。

阶段输出：

- [x] 右下角 AI 助手可用（`ai-assistant.js` + `ai_assistant.go`）
- [x] 右上角小喇叭消息中心可用（`notification-bell.js` + `notification.go`）
- [x] AI 助手可参与巡检并产出通知（`patrol.go` + `StartPatrolInspection`）

---

### 阶段 4：无人机任务分类与调度优化

目标：让无人机任务从“普通任务列表”升级为“可分类、可分级、可调度、可推荐”的任务系统。

- [ ] 为任务增加分类字段。
  - 巡检
  - 监控
  - 应急
  - 定点作业
  - 可扩展自定义分类
- [ ] 为任务增加优先级字段。
  - 高
  - 中
  - 低
  - 紧急
- [ ] 在后端建立任务分类管理逻辑。
- [ ] 在前端任务模块增加分类展示、筛选与统计。
- [ ] 建立任务调度规则。
  - 紧急任务优先
  - 低电量任务回避
  - 多无人机负载均衡
  - 同区域冲突检测
  - 同时执行数量控制
- [ ] 增加 AI 调度建议能力。
  - 推荐哪架无人机执行
  - 推荐优先执行顺序
  - 推荐是否中断当前任务
- [ ] 将任务分类结果与 AI 助手打通，支持自然语言查询任务情况。

阶段输出：

- 任务具备分类管理能力
- 任务具备优先级与调度策略
- AI 可解释调度原因

---

### 阶段 5：后端线程 / 进程并发能力建设

目标：先从架构层面建立并发执行框架，为模拟机群、航线计算、监控巡检、统计分析提供可承载能力。

- [ ] 梳理当前后端高耗时任务。
  - 模拟机状态更新
  - 航线规划计算
  - GPS / 电池数据处理
  - 日志分析
  - 通知检测
  - 统计计算
- [ ] 设计统一任务调度与执行模型。
  - 协程池
  - worker pool
  - 后台队列
  - 多进程 runner
- [ ] 为不同任务区分执行模式。
  - I/O 密集任务使用协程并发
  - CPU 密集任务独立进程或批处理
  - 实时任务优先执行
- [ ] 建立并发安全机制。
  - 任务隔离
  - 超时控制
  - 错误恢复
  - 资源上限
- [ ] 增加并发指标采集。
  - 队列长度
  - 活跃 worker 数
  - 平均耗时
  - 失败次数

阶段输出：

- 后端形成统一并发执行模型
- 为后续模块提供线程 / 进程基础能力

---

### 阶段 6：后端线程 / 进程创建与应用优化落地

目标：在上一步基础上，把并发能力真正应用到项目现有核心模块，提高系统性能和流畅度。

- [ ] 将无人机模拟任务接入并发执行池。
- [ ] 将航线规划 / 路径计算任务接入并发处理。
- [ ] 将状态监控、日志分析、告警检测改为后台并发执行。
- [ ] 将 AI 助手定时巡检改为独立后台任务。
- [ ] 将统计分析任务从同步查询改为异步计算或缓存刷新。
- [ ] 对高频 WebSocket 推送进行节流和批处理优化。
- [ ] 对数据库高频写入进行批量化或分片化优化。
- [ ] 增加并发压测与稳定性验证。

阶段输出：

- 多无人机模拟、航线计算、状态监控等任务可并行运行
- 后端吞吐和响应性明显提升

---

### 阶段 7：模拟无人机群

目标：从当前单机 demo 升级为可批量创建、7x24 小时运行、可监控、可学习的模拟无人机群系统。

- [ ] 把当前 `provider_simulated.go` 从单机逻辑改造成多实例仿真引擎。
- [ ] 支持批量创建几十个以上模拟机。
- [ ] 支持按批次启动 / 停止 / 删除模拟机。
- [ ] 支持 7x24 小时连续模拟飞行。
- [ ] 为模拟机生成完整数据。
  - GPS
  - 电池
  - 飞行阶段
  - 任务状态
  - 异常事件
- [ ] 增加异常注入能力。
  - 低电量
  - 偏航
  - 失联
  - 温度异常
  - 告警触发
- [ ] 建立模拟机管理后端接口。
- [ ] 建立模拟机前端管理模块。
  - 创建批次
  - 启停控制
  - 状态查看
  - 异常处理
- [ ] 将模拟机数据接入 AI 助手知识源。
- [ ] 在模拟飞行过程中接入强化学习框架。
  - 定义环境
  - 定义状态空间
  - 定义动作空间
  - 定义奖励函数
  - 输出策略评估结果

阶段输出：

- 支持几十台模拟机稳定运行
- 支持 7x24 连续仿真
- 支持策略学习与评估

---

### 阶段 8：数据统计与动态可视化

目标：把模拟机群、任务系统、告警系统、调度系统的数据转化为实时可视化结果，用于展示、分析和 AI 助手问答。

- [ ] 建立模拟机统计指标体系。
  - 模拟机总数
  - 在线数
  - 异常数
  - 电池风险数
  - 任务类型分布
  - 任务完成率
  - 告警类型分布
  - 策略收益变化
- [ ] 新增模拟机监控大屏或模块页。
- [ ] 增加实时图表。
  - 运行趋势图
  - 异常统计图
  - 电池风险图
  - 任务分类占比图
  - 策略学习曲线
- [ ] 通过 WebSocket 实现动态同步更新。
- [ ] 将可视化结果同步给 AI 助手，支持问答式查询。
  - 当前有多少异常模拟机
  - 哪类任务最多
  - 哪种策略更优
- [ ] 增加数据导出能力，用于竞赛答辩展示。

阶段输出：

- 模拟机与任务数据具备动态可视化展示能力
- AI 助手可直接调用统计结果进行回答

---

### 阶段 9：前端整体风格优化

目标：在功能基本成型后，统一提升界面视觉、布局层次、交互体验和竞赛展示效果。

- [ ] 重构主界面壳层视觉风格。
  - 顶部栏
  - 左侧导航
  - 右上角通知
  - 右下角 AI 助手
  - 主内容区
- [ ] 优化整体配色、阴影、卡片、按钮、图标、字体层级。
- [ ] 优化仪表盘布局，让展示更适合竞赛答辩演示。
- [ ] 优化各核心模块的一致性。
  - 无人机管理
  - 航线规划
  - 电池监控
  - 告警中心
  - 模拟机监控
- [ ] 优化交互细节。
  - 弹窗样式
  - 空状态
  - 加载状态
  - 错误提示
  - 动效与反馈
- [ ] 参考优质同类平台 UI 做展示风格升级，但保持本项目结构可落地。
- [ ] 适配桌面演示分辨率与常见笔记本屏幕。

阶段输出：

- 前端界面整体更统一、更美观、更适合竞赛展示
- AI 助手、消息中心、模拟机大盘、任务分类模块形成完整展示链路

---

## 四、最终交付目标

- [x] 数据库从 SQLite 成功迁移到 MySQL
- [x] 系统具备自动备份、手动备份、数据回滚能力
- [x] 右下角 AI 助手可用
- [x] 右上角消息通知小喇叭可用
- [ ] 无人机任务支持分类、优先级和调度优化
- [ ] 后端支持线程 / 进程并发执行
- [ ] 后端线程 / 进程能力已应用到核心业务
- [ ] 支持几十台以上模拟无人机 7x24 小时运行
- [ ] 模拟数据具备动态统计与可视化能力
- [ ] 前端整体风格完成优化，具备竞赛展示效果

---

## 五、补充说明

- 本文档是**按事件顺序推进**，不是按天拆解。
- 当前最优先的是数据库迁移和备份回滚，因为这是后续所有功能的基础。
- AI 助手和消息通知必须放在数据库与备份之后，否则后续数据接入和巡检通知都要返工。
- 模拟机、可视化、前端优化放在后半段，是因为它们依赖前面数据库、并发、任务分类和 AI/通知能力先稳定。
